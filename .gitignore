# Secrets
.env

# Virtual environment
.venv/

# Python
__pycache__/
*.pyc

# Pipeline outputs — regenerated by fetch.py / format_pptx.py, never
# hand-edited source. Keep the repo to just the pipeline code.
data.json
narrative.json
exec_summary*.pptx

# Local Claude Code settings (machine-specific permission overrides)
.claude/settings.local.json

# OS
.DS_Store
